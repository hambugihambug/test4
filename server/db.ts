/**
 * 데이터베이스 연결 설정 파일
 * 
 * 이 파일은 사용자가 직접 구현할 수 있도록 모든 DB 연동 코드가 제거되었습니다.
 * 사용자는 자신의 DB 연결 설정을 직접 구현해야 합니다.
 */

// 사용자가 직접 DB 연결 코드 구현 필요
// 예시:
// import { Pool } from 'pg';
// export const pool = new Pool({ connectionString: process.env.DATABASE_URL });
// export const db = ...;

export const pool = null; // 사용자가 구현해야 함
export const db = null;   // 사용자가 구현해야 함